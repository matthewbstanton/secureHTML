
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT USERNAME
				FROM USERS
				WHERE USERNAME LIKE 'A%';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT USERNAME
				FROM USERS
				WHERE USERNAME LIKE 'A%';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';
-----------------
SELECT DD.DOCUMENTID, DD.DESCRIPTION, DD.DOCUMENTNAME
				FROM DOCUMENTDEFINITION AS DD
				WHERE DD.PERMID = 0
				OR DD.PERMID IN (
				    SELECT PD.PERMID
				    FROM PERMISSIONDEFINITION PD
				    INNER JOIN GROUPS G ON PD.PERMID = G.PERMID
				    INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				    WHERE U.USERNAME = 'admin')
-----------------
SELECT PERMNAME
				FROM PERMISSIONDEFINITION PD
				INNER JOIN GROUPS G ON G.PERMID = PD.PERMID
				INNER JOIN USERS U ON U.GROUPID = G.GROUPID
				WHERE U.USERNAME = 'admin';